import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
    todoGrid: {
        borderTop: "1px solid rgba(0,0,0,.1)",
        paddingTop: "2rem",
        paddingBottom: "2rem",
    },
}));
